services:
  handshake:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /app/data/handshake.db
    ports:
      - "8787:8787"
    volumes:
      - ./data:/app/data
